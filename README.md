# QGenericValidator
the scope of this set of class is to provide a manager that is capable of storing a widget with the wanted trigger signal for the validation and a callback function executed for the validation in order to register many different widgets and validate all at once without the needs to manually connect everything
